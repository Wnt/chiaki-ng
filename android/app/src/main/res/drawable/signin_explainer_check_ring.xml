<?xml version="1.0" encoding="utf-8"?>
<!-- The ring around the ✓ in the mock-up: drawn twice, one at rest and one pulsing (PLE-339). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="oval">
	<solid android:color="@android:color/transparent" />
	<stroke
		android:width="2dp"
		android:color="@color/signin_explainer_highlight" />
</shape>

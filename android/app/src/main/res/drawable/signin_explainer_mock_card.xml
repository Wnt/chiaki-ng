<?xml version="1.0" encoding="utf-8"?>
<!-- The body of the drawn browser mock-up: a plain card, no brand of any browser (PLE-339). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
	<solid android:color="@color/signin_explainer_mock_surface" />
	<stroke
		android:width="1dp"
		android:color="@color/signin_explainer_mock_stroke" />
	<corners android:radius="12dp" />
</shape>
